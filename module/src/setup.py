from setuptools import setup, find_packages

setup(
    name="ranking_challenge",
    version="1.0",
    description="lib for pydantic models",
    author="PRC organizers",
    packages=find_packages(),
)
